[project]
name = "retenciones"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "duckdb>=1.5.2",
    "pandas>=3.0.3",
    "polars>=1.40.1",
    "pyarrow>=24.0.0",
    "pytest>=9.0.3",
    "rapidfuzz>=3.14.5",
    "spacy>=3.8.14",
    "sqlalchemy>=2.0.49",
]
[tool.setuptools.packages.find]
where = ["src"]

